import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'last-fifth-letter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
